# RxBridge Backend Dependencies
# Pin all versions — unpinned deps cause "works on my machine" failures and
# supply-chain attacks (malicious maintainer publishes a compromised minor version)

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
asyncpg==0.30.0

# Authentication & Security
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
cryptography==43.0.3         # pinned separately — cryptography is security-critical

# LLM
groq==0.13.0

# Scientific / ML
scikit-learn==1.5.0
pgmpy==0.1.26
networkx==3.4.2
numpy==1.26.4
joblib==1.4.2

# Cache / Redis
redis==5.0.1

# HTTP client
httpx==0.28.1

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.9      # for form data parsing
sse-starlette==2.1.3         # Server-Sent Events for real-time pipeline updates
pydantic[email]==2.10.0      # email validation in auth models
